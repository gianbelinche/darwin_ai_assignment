TELEGRAM_BOT_TOKEN=123456789:ABCdef...

# URL of the Bot Service — use the Docker service name when running via docker compose
BOT_SERVICE_URL=http://bot-service:8000

# NOTE: use postgres:// here, NOT postgresql+asyncpg:// — that scheme is only for the Python service
DATABASE_URL=postgres://expense_user:secret@postgres:5432/expenses

PORT=3000
